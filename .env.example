# Pikar-AI Environment Configuration
# ===================================
# Copy this file to .env and fill in your actual values.
# NEVER commit .env files with real secrets to version control.

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key
# IMPORTANT: Get this from Supabase Dashboard -> Project Settings -> API -> Service Role Key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Google Vertex / Gemini configuration for the Google agent service
GOOGLE_CLOUD_PROJECT=your_vertex_project_id
GOOGLE_CLOUD_LOCATION=us-central1
GOOGLE_APPLICATION_CREDENTIALS=./secrets/your-vertex-service-account.json
GOOGLE_GENAI_USE_VERTEXAI=1
GEMINI_AGENT_MODEL_PRIMARY=gemini-2.5-pro
GEMINI_AGENT_MODEL_FALLBACK=gemini-2.5-flash

# Gemini Live brainstorm voice sessions
# Server-to-server Live bridge uses Vertex in production. Check the current
# Gemini Live docs before changing the model family in deployed environments.
GEMINI_LIVE_MODEL=gemini-live-2.5-flash-native-audio
GEMINI_VOICE_NAME=Kore
GEMINI_LIVE_SILENCE_MS=500
GEMINI_LIVE_PREFIX_PADDING_MS=120
# Drops sub-speech browser noise before it reaches Gemini Live; public because
# the value is read by the frontend bundle.
NEXT_PUBLIC_VOICE_NOISE_FLOOR_RMS=0.003
NEXT_PUBLIC_VOICE_MIC_CHUNK_MS=40
NEXT_PUBLIC_VOICE_BARGE_IN_RMS=0.02
NEXT_PUBLIC_VOICE_TURN_IDLE_END_MS=500
NEXT_PUBLIC_VOICE_TURN_END_ENABLED=1
NEXT_PUBLIC_VOICE_PLAYBACK_BUFFER_MS=60
NEXT_PUBLIC_VOICE_AGENT_RESPONSE_DELAY_MS=10

# Optional local-development fallback only
# GOOGLE_API_KEY=your_gemini_api_key

# Google Workspace per-user OAuth (Phase 102)
# Required for in-app "Connect Google Workspace" flow that stores reusable
# refresh tokens in integration_credentials. DIFFERENT from GOOGLE_API_KEY
# (Vertex/Gemini API) and GOOGLE_CLIENT_ID (legacy Supabase Auth Google identity).
# Get from Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0 Client IDs.
# Recommendation: use a SEPARATE OAuth client from GOOGLE_CLIENT_ID so scope grants
# and disconnects are independent.
# GOOGLE_WORKSPACE_CLIENT_ID=your_oauth_client_id.apps.googleusercontent.com
# GOOGLE_WORKSPACE_CLIENT_SECRET=your_oauth_client_secret
# GOOGLE_WORKSPACE_REDIRECT_URI=https://your-domain.com/integrations/google_workspace/callback

# Enable Remotion rendering (requires npm/node/ffmpeg in container)
REMOTION_RENDER_ENABLED=1
REMOTION_RENDER_TIMEOUT=300

# External API Keys (optional)
# TAVILY_API_KEY=your_tavily_api_key
# FIRECRAWL_API_KEY=your_firecrawl_api_key

# Resend (transactional email + inbound forwarding)
# RESEND_API_KEY=re_your_resend_key
# RESEND_FROM_EMAIL=noreply@your-domain.com
# RESEND_WEBHOOK_SECRET=whsec_your_webhook_signing_secret
# Forward inbound emails to this address (leave blank to disable forwarding)
# RESEND_FORWARD_TO=

# Redis Cache Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_MAX_CONNECTIONS=20
# Shared outbound httpx.AsyncClient pool
OUTBOUND_HTTP_MAX_CONNECTIONS=200
OUTBOUND_HTTP_KEEPALIVE_CONNECTIONS=50
OUTBOUND_HTTP_KEEPALIVE_EXPIRY_SECONDS=30.0
OUTBOUND_HTTP_TIMEOUT_SECONDS=30.0

# =============================================================================
# STREAMING AND UPLOAD GUARDRAILS
# =============================================================================
# Maximum simultaneous SSE streams per user across chat and workflow endpoints.
SSE_MAX_CONNECTIONS_PER_USER=3
# Maximum file size accepted by /upload (bytes).
MAX_UPLOAD_SIZE_BYTES=10485760

# Storage
# LOGS_BUCKET_NAME=your-gcs-bucket-name

# Social Media OAuth (optional — only needed for social publishing features)
# LINKEDIN_CLIENT_ID=your_linkedin_client_id
# LINKEDIN_CLIENT_SECRET=your_linkedin_client_secret
# FACEBOOK_APP_ID=your_facebook_app_id
# FACEBOOK_APP_SECRET=your_facebook_app_secret
# INSTAGRAM_APP_ID=your_instagram_app_id
# INSTAGRAM_APP_SECRET=your_instagram_app_secret
# TWITTER_CLIENT_ID=your_twitter_client_id
# TWITTER_CLIENT_SECRET=your_twitter_client_secret
# YOUTUBE_CLIENT_ID=your_youtube_client_id
# YOUTUBE_CLIENT_SECRET=your_youtube_client_secret
# TIKTOK_CLIENT_KEY=your_tiktok_client_key
# TIKTOK_CLIENT_SECRET=your_tiktok_client_secret
# Pinterest API — separate OAuth client (NOT shared with any other Meta/Google entry).
# Get from: https://developers.pinterest.com/apps/
# Pinterest's token endpoint requires HTTP Basic auth (RFC 6749-strict) — the
# connector branches on PLATFORM_CONFIGS[...]['auth_method'] = 'basic'.
# PINTEREST_CLIENT_ID=your_pinterest_client_id
# PINTEREST_CLIENT_SECRET=your_pinterest_client_secret
# Meta Threads API — separate OAuth app inside the Meta App Dashboard's Threads product.
# Get from: https://developers.facebook.com/apps -> your-app -> Threads -> Configuration
# THREADS_APP_ID=your_threads_app_id
# THREADS_APP_SECRET=your_threads_app_secret

# Cloud Scheduler Authentication
# SCHEDULER_SECRET=your_scheduler_secret

# Internal service authentication (Supabase Edge Function -> FastAPI backend)
WORKFLOW_SERVICE_SECRET=your_secure_random_secret_here

# Workflow runtime safety.
WORKFLOW_STRICT_TOOL_RESOLUTION=true
WORKFLOW_STRICT_CRITICAL_TOOL_GUARD=true
WORKFLOW_ALLOW_FALLBACK_SIMULATION=false
WORKFLOW_ENFORCE_READINESS_GATE=true
BACKEND_API_URL=http://localhost:8000

# Frontend Supabase (public keys only — safe for client-side)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# LinkedIn webhook signature verification uses LINKEDIN_CLIENT_SECRET (above)
# per the Microsoft Learn LinkedIn webhooks spec. The legacy variable below is
# DEPRECATED -- it is no longer read by any runtime code as of Phase 103-02
# and may be removed from infrastructure configs in a future ops follow-up.
# LINKEDIN_WEBHOOK_SECRET=your_linkedin_webhook_secret  # DEPRECATED -- unused

DIRECTOR_RENDER_FPS=24
DIRECTOR_MAX_CONCURRENCY=4
REMOTION_RENDER_SCALE=0.5

# =============================================================================
# CONVERSATION SUMMARIZATION (long-session coherence)
# =============================================================================
# Conversation summarization for long sessions (>SESSION_MAX_EVENTS).
# Required for 30-60 minute agent tasks to retain coherence.
ENABLE_CONVERSATION_SUMMARIZER=true
SESSION_MAX_EVENTS=200

# =============================================================================
# OBSERVABILITY — SENTRY (Phase 51)
# =============================================================================
# Backend DSN — get from Sentry Dashboard -> Project -> Client Keys (DSN).
# Leave blank to disable Sentry in dev environments (no-op when unset).
# SENTRY_DSN_BACKEND=https://examplePublicKey@o0.ingest.sentry.io/0

# Sentry auth token for source map upload during CI/CD builds.
# Get from https://sentry.io/settings/auth-tokens/
# SENTRY_AUTH_TOKEN=sntrys_your_auth_token

# =============================================================================
# TEMPORARY FULL-ACCESS TESTING OVERRIDES
# =============================================================================
# Enable to bypass backend/frontend feature gates locally.
# ALLOW_ALL_FEATURES_FOR_TESTING=1
# NEXT_PUBLIC_ALLOW_ALL_FEATURES_FOR_TESTING=1
#
# Enable to show all persona-scoped workflow/initiative content while testing.
# ALLOW_ALL_PERSONA_CONTENT_FOR_TESTING=1
#
# Enable to relax persona-based request throttles for local testing.
# ALLOW_UNLIMITED_TESTING=1
# UNLIMITED_TESTING_RATE_LIMIT=100000/minute
#
# Log persona enforcement decisions instead of blocking workflow starts.
# WORKFLOW_PERSONA_ENFORCEMENT_MODE=log
#
# App Builder testing fallback when STITCH_API_KEY is not configured.
# APP_BUILDER_USE_MOCK_STITCH=1
# STITCH_API_KEY=your_stitch_api_key
